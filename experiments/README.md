# Experiments

This folder contains ad-hoc validation and exploratory plotting scripts.
These scripts are not part of the production orchestrator pipeline contract.

Environment variables used:
- `RESEARCH_HUB_PATH`: path to `[Graph_making_hub]` (optional, falls back to parent directory)
- `PROJECT_ROOT`: target project root (optional, falls back to current working directory)
